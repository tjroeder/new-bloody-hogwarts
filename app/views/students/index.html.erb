<h1>Students Index</h1>

<h2>List of Students</h2>
<% @students.order_alphabetical.each do |student| %>
  <p id="student-<%= student.id %>">Name: <%= student.name %>, Age: <%= student.age %>, House: <%= student.house %></p>
<% end %>

<h2>Average Age: <%= @students.average_age.round(1) %></h2>